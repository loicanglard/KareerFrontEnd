[build]
  command = "npm run build"
  publish = "dist"      # Vite sort dans ../dist (racine)

# SPA: redirige toutes les routes vers index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
